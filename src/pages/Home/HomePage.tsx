import React, { FC } from 'react';
import * as Styled from './styled';

const HomePage: FC = () => {
  return (
    <Styled.HomePage>
      <h1>Home page</h1>
    </Styled.HomePage>
  );
};

export default HomePage;
